<!-- The devices list itself lives in the root layout, which keeps it mounted
     across navigations so returning here costs no rebuild. This route exists
     so the URL resolves; it renders nothing of its own. -->

<!-- This page lives in the root layout's kept-alive registry, which keeps it
     mounted across navigations so returning costs no rebuild. The route
     exists so the URL resolves; it renders nothing of its own. -->

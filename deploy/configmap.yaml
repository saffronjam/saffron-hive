apiVersion: v1
kind: ConfigMap
metadata:
  name: saffron-hive
  namespace: home
  labels:
    app.kubernetes.io/name: saffron-hive
data:
  MQTT_BROKER: "tcp://192.168.1.200:1883"
  DB_PATH: "/data/saffron-hive.db"
  LISTEN_ADDR: ":8080"
